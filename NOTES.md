Anyone can download the app and form local communities but access to the global layer requires invitation from Temples of Refuge. Local Delegate Temples can also extend invitations to area-specific networks.
Delegate Temples can attest proof of humanness. 

Instead of Long list of keywords, could we ask the user to tell us a story, then we compute a semantic map to that story. If they lose their keys and need to get back in, they we can compute a semantic closeness to the story the first time they told it and let them in if they are close enough.
So instead of random words impossible to remember, they are a sequence of words with memory specific to you. Like a pass-story.
